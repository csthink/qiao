# T7 状态报告

- 任务: T7 — 端到端自测 + README 回填
- 状态: done(含评审 #2 修复:--copy e2e 覆盖)
- commit: e72082d(实现)、f35a38d(评审 #2:--copy 路径 e2e + stdout/stderr 分流)

## 改动文件
- `tests/e2e.rs`(新建)—— 6 个端到端集成测试,驱动编译出的真实二进制。
- `README.md`(改)—— 状态行改为"v1 已实现";回填安装 / 60 秒上手 / 命令表 / 本地覆盖 / bw 用法。

## 实现摘要
- **hermetic e2e**:用 `env!("CARGO_BIN_EXE_qiao")` 拿到编译产物,`Command` 跑真实命令;以
  **env 后端做 keychain 的 mock**(env 即 D3 的测试兜底),临时 `XDG_CONFIG_HOME`/`XDG_CACHE_HOME`
  隔离配置与缓存——不碰真实 keychain、不打网络。所有 key 为占位串。
- 覆盖:`env`/`code` 从 env 后端取 key 并按 S5 渲染;`list` 离线走快照(国内 .cn / /v1 印证 PROPOSAL-001 A);
  `show` 安全回归(env 变量设了占位"密钥"也绝不打印,只显引用);未知 provider 可读错误 + 非零退出;
  缺 key 的可操作错误。
- README 事实已核对(仓库 URL、overrides 路径 `~/.config/qiao/providers.toml`、`bw:item/`·`bw:id/`、
  `bw unlock --raw`)与代码/远端一致。

## 自测
```
cargo test         → 54 unit + 6 e2e passed;1 ignored(keychain 真机往返)
cargo clippy --all-targets → 无 error/warning
cargo build --release → 单二进制 5.0M;qiao --help 列全 6 子命令
```

## 对照 DoD
- [x] e2e 测试通过(keychain 可 mock)—— `tests/e2e.rs` 6 个全过,用 env 后端 + 临时 XDG 目录做 hermetic mock。
- [x] README 能让新读者跑起来 —— 含前置、`cargo install`、逐命令上手、`#profile` 多账号、本地覆盖、bw 连 Vaultwarden。

## 全局 v1 Done 核对(tasks.md)
- [x] `cargo build --release` 出单二进制,macOS 可跑 —— 5.0M。
- [x] provider 全程跑通(取 key → .env + 代码片段)—— openrouter 经 env 后端 e2e + 真机 keychain 种入验证(T6);其余 3 家配置随快照同源,路径一致。
- [ ] `bw` 后端从本地 Vaultwarden 取到 ≥1 个 key —— **需维护者真机验证**(本会话无 Vaultwarden 实例;bw 后端逻辑与未登录/锁定提示已单测)。
- [x] 全程无明文 key 落盘/进日志/进 history —— 全程 Zeroizing;show/key check 不碰明文;Err 不含 key;key set 隐藏输入不进 argv/history。
- [x] `cargo test` 全绿;`cargo clippy` 无 error。

## 评审修复记录
- **#2 --copy 路径零 e2e 覆盖** → 已修(commit f35a38d)。原 `deliver` 在 `--copy` 时只打印"已复制"、不回显片段;按评审期望改为:**片段始终写 stdout(数据)**,`--copy` 额外送剪贴板、"已复制"提示走 **stderr**(S4 SHOULD);剪贴板失败优雅降级为 stderr 提示(不让 --copy 失败,无头环境不 flaky)。新增 e2e `env_copy_prints_snippet_and_reports_copied`(exit 0、stdout 含 .env 片段、stderr 含复制结果)。真机验证:stdout 片段 / stderr 提示 / 剪贴板实写成功。e2e 由 6 增至 7。

## 偏离/疑问
- 全局 Done 里"bw 连本地 Vaultwarden 取 key"需你在有 Vaultwarden 的机器上手测一次(`bw login` → `export BW_SESSION` → 把某家 key_ref 改 `bw:item/<名>` → `qiao env <id>`)。代码路径与错误提示已就绪。
- e2e 未覆盖 `key set`(rpassword 需真实 tty,见 T6);其 keychain 写入由 T4 真机往返覆盖。
